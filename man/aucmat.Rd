% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aucmat.R
\name{aucmat}
\alias{aucmat}
\title{Screen a biomarker matrix against a binary outcome}
\usage{
aucmat(
  X,
  y,
  positive = NULL,
  ci = c("delong", "bootstrap", "none"),
  conf_level = 0.95,
  alternative = c("two.sided", "greater", "less"),
  adjust = c("BH", "holm", "bonferroni", "none"),
  na_action = c("featurewise", "complete"),
  boot_n = 2000,
  seed = NULL,
  retain_data = FALSE,
  feature_metadata = NULL
)
}
\arguments{
\item{X}{Numeric matrix or data.frame. Samples in rows, biomarkers in
columns.  Column names must be unique and non-empty.}

\item{y}{Binary outcome vector.  Logical (\code{TRUE} = positive), numeric
\code{0/1} (\code{1} = positive), factor, or character with exactly two
observed levels.}

\item{positive}{Positive class label.  Required when \code{y} is a character
or factor with ambiguous ordering.  Ignored for logical and numeric
\code{0/1} outcomes.}

\item{ci}{Confidence interval method: \code{"delong"} (default),
\code{"bootstrap"}, or \code{"none"}.}

\item{conf_level}{Confidence level in (0, 1).  Default 0.95.}

\item{alternative}{Alternative hypothesis: \code{"two.sided"} (default),
\code{"greater"}, or \code{"less"}.}

\item{adjust}{Multiplicity adjustment: \code{"BH"} (default), \code{"holm"},
\code{"bonferroni"}, or \code{"none"}.}

\item{na_action}{\code{"featurewise"} (default) uses all subjects with
observed values for each biomarker independently.  \code{"complete"} uses
only subjects with complete observations across all biomarkers.}

\item{boot_n}{Number of bootstrap replicates when \code{ci = "bootstrap"}.}

\item{seed}{Optional integer seed for bootstrap reproducibility.}

\item{retain_data}{If \code{TRUE}, retain \code{X} and \code{y} in the result object
for interactive plotting.  Default \code{FALSE}.}

\item{feature_metadata}{Optional data.frame with metadata about
biomarkers (rownames must match column names of \code{X}).}
}
\value{
An object of class \code{aucmat_screen}, a list with components:
\code{results}, \code{sample_summary}, \code{settings}, \code{call}, and optionally
\code{feature_metadata}, \code{X}, \code{y}.
}
\description{
Computes direction-preserving raw AUC, discrimination strength, and
inferential statistics for every biomarker in a numeric matrix against
a binary outcome.  The package will never silently reverse a biomarker
to force its reported AUC above 0.5.
}
\examples{
set.seed(42)
X <- matrix(rnorm(100 * 20), nrow = 100)
colnames(X) <- paste0("bm", 1:20)
y <- rep(c(0, 1), each = 50)
fit <- aucmat(X, y)
print(fit)
head(as.data.frame(fit))
}
