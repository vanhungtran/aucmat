% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-panel.R
\name{plot_roc_panel}
\alias{plot_roc_panel}
\title{ROC curve for a fitted biomarker panel vs. its individual components}
\usage{
plot_roc_panel(
  panel,
  X,
  y,
  positive = NULL,
  biomarkers = NULL,
  n_top = 3L,
  use = c("cv", "train"),
  add_ci = TRUE,
  boot_n = 500,
  seed = NULL
)
}
\arguments{
\item{panel}{An \code{aucmat_panel} object from \code{\link[=fit_auc_panel]{fit_auc_panel()}}.}

\item{X}{Numeric matrix -- the same data used to fit \code{panel} (or a
superset of its columns).}

\item{y}{Binary outcome -- the same outcome used to fit \code{panel}.}

\item{positive}{Optional positive class label (see \code{\link[=aucmat]{aucmat()}}).}

\item{biomarkers}{Character vector of individual biomarkers to overlay.
Default: the top \code{n_top} panel components by their own univariate AUC.}

\item{n_top}{Number of individual biomarkers to overlay when
\code{biomarkers} is not supplied. Default 3.}

\item{use}{\code{"cv"} (default) plots the honest cross-validated panel
score; \code{"train"} plots the in-sample (optimistic) score. Falls back
to \code{"train"} with a warning if \code{panel} was fit with \code{n_folds < 2}.}

\item{add_ci}{Add bootstrap sensitivity confidence ribbons. Default
\code{TRUE} -- set \code{FALSE} for a faster, ribbon-free plot.}

\item{boot_n}{Bootstrap replicates for CI ribbons. Default 500.}

\item{seed}{Optional seed for CI bootstrap reproducibility.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots the empirical ROC curve of a combined panel score -- the honest
out-of-fold score by default -- together with the ROC curves of its
leading individual biomarkers, for a direct visual read on whether
combining biomarkers improved discrimination.
}
\examples{
\donttest{
set.seed(42)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65, 0.60),
  correlation = 0.3, structure = "exchangeable")
X <- as.matrix(sim$data[, 1:4]); y <- sim$data$truth
panel <- fit_auc_panel(X, y, method = "ridge", n_folds = 5, seed = 1)
plot_roc_panel(panel, X, y)
}
}
