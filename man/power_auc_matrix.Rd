% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power-analysis.R
\name{power_auc_matrix}
\alias{power_auc_matrix}
\title{Power and sample size for AUC comparisons}
\usage{
power_auc_matrix(
  target_aucs,
  null_auc = 0.5,
  correlation = 0,
  prevalence = 0.5,
  power = NULL,
  n = NULL,
  alpha = 0.05,
  alternative = c("two.sided", "greater", "less"),
  adjust = c("none", "BH", "bonferroni")
)
}
\arguments{
\item{target_aucs}{Numeric vector of AUC values under the alternative.
The null value (chance) is \code{null_auc}.}

\item{null_auc}{Null AUC value.  Default 0.5.}

\item{correlation}{Between-biomarker correlation (scalar for exchangeable).
Default 0.}

\item{prevalence}{Expected prevalence in (0, 1).}

\item{power}{Desired power.  If \code{NULL} (default), compute power for
given \code{n}.  If supplied, compute required \code{n}.}

\item{n}{Sample size.  Required when \code{power = NULL}.  Ignored otherwise.}

\item{alpha}{Significance level.  Default 0.05.}

\item{alternative}{\code{"two.sided"} (default), \code{"greater"}, or \code{"less"}.}

\item{adjust}{Multiplicity adjustment: \code{"none"} (default), \code{"BH"},
\code{"bonferroni"}.}
}
\value{
A list of class \code{aucmat_power} with components \code{n}, \code{power},
\code{target_aucs}, \code{effect_sizes}, \code{alpha}, \code{adjusted_alpha}.
}
\description{
Computes the required sample size or achievable power for comparing AUCs
of correlated biomarkers via DeLong's test.  Uses the binormal model to
translate target AUCs into mean separations and the DeLong variance
structure for correlated ROC curves.
}
\examples{
# Power for n=200, detecting AUC=0.70 vs chance
power_auc_matrix(target_aucs = c(0.70, 0.65), n = 200,
  prevalence = 0.3, correlation = 0.2)

# Sample size for 80\% power
\donttest{
power_auc_matrix(target_aucs = 0.70, power = 0.80,
  prevalence = 0.3)
}
}
