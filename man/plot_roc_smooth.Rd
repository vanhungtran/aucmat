% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc-smooth.R
\name{plot_roc_smooth}
\alias{plot_roc_smooth}
\title{Plot smoothed ROC curves for selected biomarkers}
\usage{
plot_roc_smooth(
  fit,
  X,
  y,
  biomarkers = NULL,
  smooth_method = c("binormal", "density"),
  show_empirical = TRUE,
  add_ci = TRUE,
  boot_n = 500
)
}
\arguments{
\item{fit}{An \code{aucmat_screen} object.}

\item{X}{Numeric matrix.}

\item{y}{Binary outcome.}

\item{biomarkers}{Character vector.  Default: top 6 by AUC.}

\item{smooth_method}{\code{"binormal"} (default) or \code{"density"}.}

\item{show_empirical}{Also plot the empirical (step) ROC curve.
Default \code{TRUE}.}

\item{add_ci}{Add bootstrap CI ribbons.  Default \code{TRUE}.}

\item{boot_n}{Bootstrap replicates for CI ribbons.}
}
\value{
A \code{ggplot2} object.
}
\description{
As \code{\link[=plot_roc_top]{plot_roc_top()}} but adds a binormal-smoothed ROC curve overlay
using \code{\link[pROC:smooth]{pROC::smooth()}}.  The smoothed curve reduces step-artifact
noise and is the standard for publication-quality ROC figures.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65),
  correlation = 0.3, structure = "exchangeable")
fit <- aucmat(as.matrix(sim$data[, 1:3]), sim$data$truth, ci = "none")
plot_roc_smooth(fit, as.matrix(sim$data[, 1:3]), sim$data$truth)
}
}
