% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiclass-auc.R
\name{aucmat_multiclass}
\alias{aucmat_multiclass}
\title{Screen biomarkers against a multiclass outcome}
\usage{
aucmat_multiclass(
  X,
  y,
  ci = c("none", "bootstrap"),
  conf_level = 0.95,
  boot_n = 2000,
  seed = NULL,
  adjust = c("none", "BH", "holm", "bonferroni")
)
}
\arguments{
\item{X}{Numeric matrix (n x p).}

\item{y}{Outcome with 3+ classes.  Factor, character, or integer.}

\item{ci}{\code{"none"} (default) or \code{"bootstrap"}.  DeLong is not defined
for multiclass.}

\item{conf_level}{Confidence level.}

\item{boot_n}{Bootstrap replicates when \code{ci = "bootstrap"}.}

\item{seed}{Optional seed.}

\item{adjust}{Multiplicity adjustment across all biomarker-class pairs.}
}
\value{
A list of class \code{aucmat_multiclass} with components \code{ovr}
(one-vs-rest), \code{hand_till} (one value per biomarker), \code{classes},
\code{settings}.
}
\description{
Computes one-versus-rest AUC for each biomarker-class pair, plus
Hand-Till multiclass AUC per biomarker.  Hand-Till is the average AUC
of all pairwise class comparisons weighted by class prevalence.
}
\examples{
\donttest{
set.seed(1)
# 3-class outcome with 2 biomarkers
n <- 300
y <- sample(c("A", "B", "C"), n, replace = TRUE, prob = c(0.5, 0.3, 0.2))
X <- cbind(
  X1 = rnorm(n) + ifelse(y == "A", 1.5, ifelse(y == "B", 0.5, -1)),
  X2 = rnorm(n) + ifelse(y == "C", 2.0, 0)
)
colnames(X) <- c("bm1", "bm2")
res <- aucmat_multiclass(X, y, ci = "none")
print(res)
}
}
