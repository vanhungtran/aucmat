% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-simulation.R
\name{validate_simulation}
\alias{validate_simulation}
\title{Validate a \code{simulate_auc_matrix()} specification by repeated simulation}
\usage{
validate_simulation(
  ...,
  times = 100,
  tolerance = list(auc = 0.02, correlation = 0.05),
  seed = NULL
)
}
\arguments{
\item{...}{Arguments forwarded to \code{\link[=simulate_auc_matrix]{simulate_auc_matrix()}} on every
replicate (e.g. \code{n}, \code{prevalence}, \code{target_aucs}, \code{correlation},
\code{structure}, \code{feasibility}).  The replicate stream is controlled by
\code{validate_simulation()}'s own \code{seed} argument, below.}

\item{times}{Number of independent replicates.  Default 100.}

\item{tolerance}{Named list with \code{auc} and \code{correlation} tolerances used
for the target-interval hit rate.  Default
\code{list(auc = 0.02, correlation = 0.05)}.}

\item{seed}{Optional integer seed controlling the replicate stream.
Each replicate uses seed \code{seed + replicate_index - 1} when supplied.}
}
\value{
An object of class \code{aucmat_simulation_validation}, a list with
components \code{auc_bias}, \code{auc_rmse}, \code{auc_mc_se}, \code{auc_hit_rate},
\code{corr_bias}, \code{corr_rmse}, \code{corr_mc_se}, \code{corr_hit_rate}, \code{n_requested},
\code{n_ok}, \code{n_failed}, \code{settings}.
}
\description{
Repeats a \code{\link[=simulate_auc_matrix]{simulate_auc_matrix()}} call \code{times} times with independent
seeds and reports how closely the achieved AUCs and correlations track
their targets, with Monte Carlo-aware uncertainty.  A single simulated
draw is never sufficient evidence that a simulator is calibrated; this
function is the check.
}
\examples{
\donttest{
val <- validate_simulation(
  n = 200, prevalence = 0.3,
  target_aucs = c(0.8, 0.7), correlation = 0.3, structure = "exchangeable",
  times = 50, seed = 1
)
print(val)
}
}
