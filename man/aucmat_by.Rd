% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group-screening.R
\name{aucmat_by}
\alias{aucmat_by}
\title{Stratified biomarker screening by group}
\usage{
aucmat_by(
  X,
  y,
  group,
  positive = NULL,
  ci = c("delong", "bootstrap", "none"),
  conf_level = 0.95,
  adjust = "BH",
  boot_n = 2000,
  min_group_size = 10L
)
}
\arguments{
\item{X}{Numeric matrix.}

\item{y}{Binary outcome.}

\item{group}{Factor or vector coercible to factor.  Screening is
performed within each level.}

\item{positive}{Optional positive class label.}

\item{ci, conf_level, adjust, boot_n}{Passed to \code{\link[=aucmat]{aucmat()}}.}

\item{min_group_size}{Minimum number of observations per group
(both classes required).  Default 10.}
}
\value{
A list of class \code{aucmat_by} with \code{results} (per-group data.frame),
\code{heterogeneity} (I-squared, Q, p per biomarker), \code{groups}, \code{settings}.
}
\description{
Runs \code{\link[=aucmat]{aucmat()}} within each level of a grouping variable and returns
per-group results plus a heterogeneity summary (I-squared, Cochran's Q).
Useful for assessing whether biomarker discrimination varies by batch,
site, or clinical stratum.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.8, 0.7, 0.6), correlation = 0.3,
  structure = "exchangeable")
grp <- sample(c("SiteA", "SiteB"), 300, replace = TRUE)
res <- aucmat_by(as.matrix(sim$data[, 1:3]), sim$data$truth, grp,
  ci = "none")
print(res)
}
}
