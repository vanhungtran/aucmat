% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv-screening.R
\name{cv_aucmat}
\alias{cv_aucmat}
\title{Cross-validated AUC screening}
\usage{
cv_aucmat(
  X,
  y,
  positive = NULL,
  n_folds = 5L,
  n_repeats = 1L,
  seed = NULL,
  ci = c("none", "delong"),
  conf_level = 0.95
)
}
\arguments{
\item{X}{Numeric matrix (n  x  p).}

\item{y}{Binary outcome.}

\item{positive}{Optional positive class label.}

\item{n_folds}{Number of cross-validation folds.  Default 5.}

\item{n_repeats}{Number of repeats of the full CV.  Default 1.}

\item{seed}{Seed for fold assignment reproducibility.}

\item{ci}{CI method for final pooled AUCs: \code{"delong"} or \code{"none"}.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{aucmat_cv} with \code{results}, \code{folds}, \code{settings}.
}
\description{
Computes out-of-fold AUCs for every biomarker via stratified v-fold
cross-validation.  Each biomarker's pooled out-of-fold predictions across
all folds are used to compute a single honest AUC that is not inflated by
same-data overfitting.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 200, prevalence = 0.3,
  target_aucs = c(0.8, 0.7, 0.6), correlation = 0.3,
  structure = "exchangeable")
cv <- cv_aucmat(as.matrix(sim$data[, 1:3]), sim$data$truth,
  n_folds = 3, seed = 42)
print(cv)
}
}
