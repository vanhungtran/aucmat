% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-scores.R
\name{compare_panel_auc}
\alias{compare_panel_auc}
\title{Test whether a combined panel improves on its individual biomarkers}
\usage{
compare_panel_auc(
  panel,
  X,
  y,
  positive = NULL,
  biomarkers = NULL,
  use = c("cv", "train"),
  ...
)
}
\arguments{
\item{panel}{An \code{aucmat_panel} object from \code{\link[=fit_auc_panel]{fit_auc_panel()}}.}

\item{X, y}{The same data (and outcome) used to fit \code{panel}.}

\item{positive}{Optional positive class label.}

\item{biomarkers}{Character vector of individual biomarkers to compare
against. Default: all biomarkers used in the panel.}

\item{use}{\code{"cv"} (default) compares the honest cross-validated panel
score; \code{"train"} compares the in-sample (optimistic) score. Errors if
\code{"cv"} is requested but \code{panel} was fit with \code{n_folds < 2}.}

\item{...}{Passed to \code{\link[=compare_auc]{compare_auc()}} (\code{hypothesis}, \code{alternative},
\code{margin}, \code{conf_level}, \code{method}, \code{boot_n}, \code{seed}, \code{adjust}).}
}
\value{
A data.frame of class \code{aucmat_compare} (see \code{\link[=compare_auc]{compare_auc()}}).
\code{biomarker_a} is always the panel score.
}
\description{
Treats the panel's honest cross-validated score as one more "biomarker"
and reuses \code{\link[=compare_auc]{compare_auc()}}'s DeLong or bootstrap paired-comparison engine
to test it against each individual biomarker used to build the panel, on
the same subjects. Answers "did combining help?" with a confidence
interval and p-value rather than a side-by-side comparison of two point
estimates.
}
\examples{
\donttest{
set.seed(42)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65, 0.60),
  correlation = 0.3, structure = "exchangeable")
X <- as.matrix(sim$data[, 1:4]); y <- sim$data$truth
panel <- fit_auc_panel(X, y, method = "ridge", n_folds = 5, seed = 1)
compare_panel_auc(panel, X, y)
}
}
