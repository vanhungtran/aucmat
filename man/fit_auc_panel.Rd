% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-scores.R
\name{fit_auc_panel}
\alias{fit_auc_panel}
\title{Fit and validate a multivariable biomarker panel}
\usage{
fit_auc_panel(
  X,
  y,
  positive = NULL,
  method = c("ridge", "lasso", "elasticnet", "logistic", "su_liu", "unweighted"),
  alpha = NULL,
  shrinkage = 0.1,
  n_folds = 5L,
  seed = NULL,
  standardize = TRUE
)
}
\arguments{
\item{X}{Numeric matrix (n x p).}

\item{y}{Binary outcome.}

\item{positive}{Optional positive class label.}

\item{method}{\code{"ridge"} (default), \code{"lasso"}, \code{"elasticnet"}, \code{"logistic"}
(unpenalized), \code{"su_liu"} (closed-form AUC-maximizing linear
combination under the binormal model), or \code{"unweighted"} (equal-weight
sum of direction-aligned standardized biomarkers; no fitting).}

\item{alpha}{Elastic-net mixing: 0 = ridge, 1 = lasso. Only used when
\code{method = "elasticnet"}; defaults to 0.5 (a true ridge/lasso blend)
when not supplied. Ignored for other methods.}

\item{shrinkage}{Shrinkage intensity in \verb{[0, 1]} toward the diagonal
covariance, used only by \code{method = "su_liu"}. \code{0} uses the raw pooled
covariance (unstable or infeasible once \code{p} approaches \code{n}); \code{1}
shrinks all the way to a diagonal (independence) covariance. Default
0.1.}

\item{n_folds}{Outer CV folds for the honest AUC (0 = no CV, uses
training AUC only). Default 5.}

\item{seed}{Optional seed for CV fold-assignment reproducibility.}

\item{standardize}{Center/scale predictors before fitting. Default
\code{TRUE}. Centering and scaling are always computed from the relevant
training data only (the full data for the final model; each outer
training fold for the honest CV score), never from held-out rows.}
}
\value{
A list of class \code{aucmat_panel} with components:
\code{coefficients}, \code{auc_train}, \code{auc_cv}, \code{fitted_model} (final-model
fit object together with the centering/scaling used to predict on new
data via \code{\link[=predict.aucmat_panel]{predict.aucmat_panel()}}), \code{predictions}, \code{settings},
\code{sample_summary}.
}
\description{
Builds a combined biomarker score and reports both an in-sample AUC and
an honest, nested cross-validated AUC. "Nested" means every step that
looks at the outcome -- penalty selection for the penalized methods, and
centering/scaling for all methods -- is re-derived from the training
portion of each outer fold alone, never from data that includes the held
-out test rows. This prevents the optimistic bias that comes from tuning
on the full data and only refitting coefficients per fold.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65, 0.60),
  correlation = 0.3, structure = "exchangeable")
X <- as.matrix(sim$data[, 1:4]); y <- sim$data$truth

panel <- fit_auc_panel(X, y, method = "ridge", n_folds = 3, seed = 42)
print(panel)

# Closed-form AUC-maximizing combination -- no glmnet dependency
panel_su <- fit_auc_panel(X, y, method = "su_liu", n_folds = 3, seed = 42)

# Assumption-light equal-weight baseline
panel_uw <- fit_auc_panel(X, y, method = "unweighted", n_folds = 3, seed = 42)
}
}
