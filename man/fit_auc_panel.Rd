% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-scores.R
\name{fit_auc_panel}
\alias{fit_auc_panel}
\title{Fit and validate a multivariable biomarker panel}
\usage{
fit_auc_panel(
  X,
  y,
  positive = NULL,
  method = c("ridge", "lasso", "elasticnet", "logistic"),
  alpha = 0,
  n_folds = 5L,
  seed = NULL,
  standardize = TRUE
)
}
\arguments{
\item{X}{Numeric matrix (n × p).}

\item{y}{Binary outcome.}

\item{positive}{Optional positive class label.}

\item{method}{\code{"ridge"} (default), \code{"lasso"}, \code{"elasticnet"}, or
\code{"logistic"} (unpenalized).}

\item{alpha}{Elastic-net mixing: 0 = ridge, 1 = lasso.  Default 0
(ridge). Only used when \code{method = "elasticnet"}.}

\item{n_folds}{CV folds for honest AUC (0 = no CV, uses training AUC).
Default 5.}

\item{seed}{Optional seed for CV reproducibility.}

\item{standardize}{Standardize predictors before fitting.  Default \code{TRUE}.}
}
\value{
A list of class \code{aucmat_panel} with components:
\code{coefficients}, \code{auc_train}, \code{auc_cv}, \code{fitted_model}, \code{predictions},
\code{settings}.
}
\description{
Builds a combined biomarker score via penalized logistic regression
(ridge, lasso, or elastic-net) with optional cross-validation for
honest performance estimation.  Returns the fitted model, coefficients,
and performance metrics on both training and validation data.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65, 0.60),
  correlation = 0.3, structure = "exchangeable")
panel <- fit_auc_panel(as.matrix(sim$data[, 1:4]), sim$data$truth,
  method = "ridge", n_folds = 3, seed = 42)
print(panel)
}
}
