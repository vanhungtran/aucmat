% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{auc_stability}
\alias{auc_stability}
\title{Bootstrap rank-stability analysis for biomarker screening}
\usage{
auc_stability(
  X,
  y,
  positive = NULL,
  times = 1000,
  top_k = c(10, 25, 50),
  seed = NULL,
  max_pairs_for_coselection = 20
)
}
\arguments{
\item{X}{Numeric matrix (samples in rows, biomarkers in columns).}

\item{y}{Binary outcome vector.}

\item{positive}{Optional positive class label.}

\item{times}{Number of bootstrap replicates.  Default 1000.}

\item{top_k}{Integer vector of set sizes for top-k probability
reporting.  Default \code{c(10, 25, 50)}.}

\item{seed}{Integer seed for reproducibility.}

\item{max_pairs_for_coselection}{Maximum number of top biomarkers for
which pairwise co-selection frequencies are reported.  Default 20.}
}
\value{
An object of class \code{aucmat_stability}, a list with components:
\code{rank_summary}, \code{top_k_probs}, \code{coselection}, \code{settings}.
}
\description{
Resamples positive and negative subjects separately with replacement,
recomputes the complete feature screen for each replicate, and
aggregates rank distributions to assess the stability of leading
biomarkers.
}
\examples{
\donttest{
set.seed(42)
X <- matrix(rnorm(200*10), 200, 10, dimnames=list(NULL, paste0("bm",1:10)))
y <- rep(c(0,1), each=100)
stab <- auc_stability(X, y, times=50, seed=1)
print(stab)
}
}
