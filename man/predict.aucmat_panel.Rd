% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-scores.R
\name{predict.aucmat_panel}
\alias{predict.aucmat_panel}
\title{Predict panel scores for new subjects}
\usage{
\method{predict}{aucmat_panel}(object, newdata, ...)
}
\arguments{
\item{object}{An \code{aucmat_panel} object from \code{\link[=fit_auc_panel]{fit_auc_panel()}}.}

\item{newdata}{Numeric matrix or data.frame containing at least the
columns used to fit \code{object}.}

\item{...}{Ignored.}
}
\value{
A numeric vector of panel scores, one per row of \code{newdata}.
}
\description{
Applies a fitted panel's coefficients to new data, using the
centering/scaling stored from the original (training) fit.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(n = 300, prevalence = 0.3,
  target_aucs = c(0.85, 0.75, 0.65, 0.60),
  correlation = 0.3, structure = "exchangeable")
X <- as.matrix(sim$data[, 1:4]); y <- sim$data$truth
panel <- fit_auc_panel(X[1:200, ], y[1:200], method = "ridge", n_folds = 0)
predict(panel, X[201:300, ])
}
}
