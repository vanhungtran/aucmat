% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc-smooth.R
\name{roc_test}
\alias{roc_test}
\title{Single-biomarker ROC test against chance or another biomarker}
\usage{
roc_test(
  x,
  y,
  X = NULL,
  x2 = NULL,
  positive = NULL,
  alternative = c("two.sided", "greater", "less"),
  method = c("delong", "bootstrap"),
  boot_n = 2000,
  conf_level = 0.95
)
}
\arguments{
\item{x}{Numeric biomarker vector (or matrix column name if X supplied).}

\item{y}{Binary outcome.}

\item{X}{Optional numeric matrix.  If supplied, \code{x} and \code{x2} are column
names or indices.}

\item{x2}{Optional second biomarker for paired comparison.}

\item{positive}{Positive class label.}

\item{alternative}{\code{"two.sided"} (default), \code{"greater"}, or \code{"less"}.}

\item{method}{\code{"delong"} (default) or \code{"bootstrap"}.}

\item{boot_n}{Bootstrap replicates when \code{method = "bootstrap"}.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{aucmat_roc_test} with \code{auc}, \code{se}, \code{ci},
\code{p_value}, \code{n}, \code{method}.
}
\description{
A focused wrapper around DeLong's test for one or two biomarkers on
common subjects.  Returns a clean summary suitable for reporting.
}
\examples{
set.seed(1)
y <- rbinom(100, 1, 0.3)
x <- rnorm(100) + y * 1.5
roc_test(x, y)
}
