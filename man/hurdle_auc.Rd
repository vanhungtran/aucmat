% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle-auc.R
\name{hurdle_auc}
\alias{hurdle_auc}
\title{Hurdle-AUC for zero-inflated biomarkers}
\usage{
hurdle_auc(X, y, positive = NULL, zero_threshold = 0)
}
\arguments{
\item{X}{Numeric matrix (n x p).  Zeros are treated as the hurdle.}

\item{y}{Binary outcome.}

\item{positive}{Optional positive class label.}

\item{zero_threshold}{Values at or below this are treated as zero.
Default 0.}
}
\value{
A list of class \code{aucmat_hurdle} with components:
\code{results} (data.frame with per-biomarker hurdle diagnostics),
\code{stage1_models} (list of logistic regression fits),
\code{settings}.
}
\description{
Computes a two-stage Hurdle-AUC for data where a large fraction of
values are exactly zero (e.g., scRNA-seq, microbiome, mass-spec with
detection limits).  Stage 1 models zero vs non-zero via logistic
regression; Stage 2 computes standard AUC on the non-zero values only.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_hurdle_auc(n = 300, prevalence = 0.3,
  target_hurdle_aucs = c(0.85, 0.72, 0.55),
  zero_rate_neg = c(0.55, 0.30, 0.80),
  zero_rate_pos = c(0.25, 0.10, 0.70))
X <- as.matrix(sim$data[, 1:3])
y <- sim$data$truth
res <- hurdle_auc(X, y)
print(res)
}
}
