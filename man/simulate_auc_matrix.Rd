% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-auc-matrix.R
\name{simulate_auc_matrix}
\alias{simulate_auc_matrix}
\title{Simulate a biomarker matrix with target AUCs and a parametrized
correlation structure}
\usage{
simulate_auc_matrix(
  n = NULL,
  prevalence = NULL,
  y = NULL,
  positive = NULL,
  outcome_mode = c("exact", "bernoulli"),
  target_aucs,
  correlation = NULL,
  structure = c("user", "exchangeable", "ar1", "block"),
  block_sizes = NULL,
  rho_within = NULL,
  rho_between = NULL,
  feasibility = c("error", "nearest"),
  seed = NULL,
  verify = TRUE
)
}
\arguments{
\item{n}{Number of observations (generated-outcome mode).}

\item{prevalence}{Proportion of positive class, in (0, 1)
(generated-outcome mode).}

\item{y}{Binary outcome vector (supplied-outcome mode).  Accepted inputs
are logical, numeric 0/1, factor, or character with exactly two
non-missing classes.}

\item{positive}{Optional positive class label when \code{y} is a factor or
character (supplied-outcome mode).}

\item{outcome_mode}{\code{"exact"} (default) or \code{"bernoulli"}.  Only used
when generating \code{y} from \code{n}/\code{prevalence}.}

\item{target_aucs}{Numeric vector of target AUC values, each in (0, 1).}

\item{correlation}{For \code{structure = "user"}, the full p x p target
correlation matrix.  For \code{"exchangeable"} or \code{"ar1"}, a single
correlation value in (-1, 1).  Unused for \code{"block"}.}

\item{structure}{One of \code{"user"} (default), \code{"exchangeable"}, \code{"ar1"},
or \code{"block"}.}

\item{block_sizes}{Integer vector of block sizes summing to
\code{length(target_aucs)}.  Required when \code{structure = "block"}.}

\item{rho_within, rho_between}{Within- and between-block correlations.
Required when \code{structure = "block"}.}

\item{feasibility}{\code{"error"} (default) or \code{"nearest"}.  When the
requested p x p correlation matrix is not positive definite, \code{"error"}
stops with a structured \code{aucmat_infeasible_targets} condition;
\code{"nearest"} projects to the nearest positive-semidefinite matrix and
proceeds, reporting the adjustment.}

\item{seed}{Optional integer seed.}

\item{verify}{Logical.  If \code{TRUE} (default), empirically verify achieved
AUCs and correlations on the generated sample.}
}
\value{
An object of class \code{aucmat_simulation}, a list with components
\code{data}, \code{target_aucs}, \code{achieved_aucs}, \code{requested_correlation},
\code{achieved_correlation}, \code{mean_shift}, \code{Sigma}, \code{feasibility}, \code{n},
\code{prevalence}, \code{seed}.  \code{achieved_correlation} is the \strong{within-class}
correlation (each class centred on its own empirical mean before
correlating), matching the scope of \code{requested_correlation}.  The raw
pooled correlation of \verb{data$X1, data$X2, ...} (ignoring \code{truth}) will
be systematically higher whenever the corresponding AUCs are above
0.5, because mixing two subpopulations with different means inflates
pooled correlation independently of the within-class dependence
structure.
}
\description{
Generates correlated biomarkers from a class-conditional multivariate
normal: \code{X | Y = 1 ~ MVN(mu_1, R)} and \code{X | Y = 0 ~ MVN(mu_0, R)}, with
the same p x p correlation matrix \code{R} in both classes and a per-biomarker
mean shift \code{mu_1 - mu_0} calibrated in closed form from \code{target_aucs}
(the equal-variance binormal relationship also used by
\code{\link[=generate_data_analytical]{generate_data_analytical()}}).  \code{R} is built from a named structure
instead of requiring a hand-supplied matrix, and feasibility is reported
as structured diagnostics instead of a bare warning.
}
\details{
Exactly one of (\code{y}, \code{positive}) or (\code{n}, \code{prevalence}) must be supplied.
Supplying \code{y} retains its exact values, class counts, and row order.
Generating \code{n}/\code{prevalence} defaults to exact rounded class counts with
randomised row order (\code{outcome_mode = "exact"}); pass
\code{outcome_mode = "bernoulli"} for independent Bernoulli draws with random
class counts.  Because the outcome is fixed before any biomarker is
drawn, both modes deliver the outcome's class counts exactly -- no
biomarker is generated by thresholding a latent variable.
}
\examples{
\donttest{
set.seed(1)
sim <- simulate_auc_matrix(
  n = 500, prevalence = 0.3,
  target_aucs = c(0.9, 0.8, 0.7, 0.65),
  correlation = 0.4, structure = "exchangeable"
)
data.frame(target = sim$target_aucs, achieved = round(sim$achieved_aucs, 3))
}
}
